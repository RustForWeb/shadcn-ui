mod components;

use leptos::prelude::*;

// TODO: remove allow
#[allow(dead_code)]
pub fn render() -> impl IntoView {
    let children: Vec<AnyView> = vec![];

    // TODO: component routes

    children.into_view()
}
